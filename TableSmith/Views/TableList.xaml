<Controls:MetroWindow x:Class="TableSmith.Views.TableList"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      mc:Ignorable="d"
                      Title="TableSmith: テーブル一覧"
                      Icon="pack://application:,,,/Resources/Images/TableSmith.ico"
                      Height="650"
                      Width="1100"
                      MinHeight="560"
                      MinWidth="920"
                      WindowStartupLocation="CenterOwner"
                      GlowBrush="DeepSkyBlue"
                      BorderThickness="1"
                      Background="#F7F9FC">
    <Controls:MetroWindow.Resources>
        <Style x:Key="WrappingColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="6,5" />
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   LineHeight="16" />
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Controls:MetroWindow.Resources>

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="ファイル">
                <MenuItem Header="閉じる" Click="CloseMenuItem_Click" />
            </MenuItem>
        </Menu>

        <Grid Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="330" />
                <ColumnDefinition Width="14" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <GroupBox Header="テーブル一覧" Padding="12">
                <DataGrid x:Name="TableDataGrid"
                          ItemsSource="{Binding Tables}"
                          SelectedItem="{Binding SelectedTable}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserSortColumns="False"
                          HeadersVisibility="Column"
                          ColumnHeaderStyle="{StaticResource WrappingColumnHeaderStyle}"
                          GridLinesVisibility="Horizontal"
                          RowHeaderWidth="0"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          AlternatingRowBackground="#EEF4FB">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="物理名" Binding="{Binding TableName}" Width="140" IsReadOnly="True" />
                        <DataGridTextColumn Header="論理名" Binding="{Binding TableDisplayName}" Width="*" IsReadOnly="True" />
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <GroupBox Header="テーブル情報" Padding="14" Margin="0,0,0,14">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="テーブル物理名" VerticalAlignment="Center" Margin="0,0,10,10" />
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SelectedTable.TableName}" IsReadOnly="True" Height="30" Margin="0,0,24,10" />

                        <TextBlock Grid.Row="0" Grid.Column="2" Text="テーブル論理名" VerticalAlignment="Center" Margin="0,0,10,10" />
                        <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding SelectedTable.TableDisplayName}" IsReadOnly="True" Height="30" Margin="0,0,0,10" />

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="説明" VerticalAlignment="Top" Margin="0,4,10,0" />
                        <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3"
                                 Text="{Binding SelectedTable.Description}"
                                 IsReadOnly="True"
                                 TextWrapping="Wrap"
                                 Height="70"
                                 VerticalScrollBarVisibility="Auto" />
                    </Grid>
                </GroupBox>

                <GroupBox Grid.Row="1" Header="カラム情報" Padding="14">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Button Content="?"
                                Width="32"
                                Height="28"
                                HorizontalAlignment="Right"
                                Margin="0,0,0,10"
                                FontWeight="Bold"
                                Click="ColumnHelpButton_Click"
                                ToolTip="カラム設定項目の説明を表示します。" />

                        <DataGrid Grid.Row="1"
                              ItemsSource="{Binding SelectedTable.Columns}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              CanUserSortColumns="False"
                              HeadersVisibility="Column"
                              ColumnHeaderStyle="{StaticResource WrappingColumnHeaderStyle}"
                              GridLinesVisibility="Horizontal"
                              RowHeaderWidth="0"
                              AlternatingRowBackground="#EEF4FB"
                              SelectionMode="Single"
                              SelectionUnit="FullRow">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="No" Binding="{Binding No}" Width="55" IsReadOnly="True" />
                            <DataGridTextColumn Header="物理名" Binding="{Binding ColumnName}" Width="150" IsReadOnly="True" />
                            <DataGridTextColumn Header="論理名" Binding="{Binding ColumnDisplayName}" Width="160" IsReadOnly="True" />
                            <DataGridTextColumn Header="型" Binding="{Binding DataType}" Width="120" IsReadOnly="True" />
                            <DataGridTextColumn Header="サイズ" Binding="{Binding DataSize}" Width="80" IsReadOnly="True" />
                            <DataGridCheckBoxColumn Header="PK" Binding="{Binding IsPrimaryKey}" Width="60" IsReadOnly="True" />
                            <DataGridCheckBoxColumn Header="Not Null" Binding="{Binding IsNotNull}" Width="85" IsReadOnly="True" />
                            <DataGridCheckBoxColumn Header="FK" Binding="{Binding IsForeignKey}" Width="60" IsReadOnly="True" />
                            <DataGridTextColumn Header="参照PK" Binding="{Binding ForeignKeyDisplayName}" Width="150" IsReadOnly="True" />
                            <DataGridTextColumn Header="既定値" Binding="{Binding DefaultValue}" Width="110" IsReadOnly="True" />
                            <DataGridTextColumn Header="説明" Binding="{Binding Description}" Width="*" MinWidth="160" IsReadOnly="True" />
                        </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </GroupBox>
            </Grid>

            <StackPanel Grid.Row="1" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
                <Button Content="編集" Width="100" Margin="0,0,8,0" Click="EditButton_Click" />
                <Button Content="閉じる" Width="100" Click="CloseButton_Click" />
            </StackPanel>
        </Grid>
    </DockPanel>
</Controls:MetroWindow>
