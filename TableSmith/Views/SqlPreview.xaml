<Controls:MetroWindow x:Class="TableSmith.Views.SqlPreview"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      Title="TableSmith: CREATE TABLE"
                      Icon="pack://application:,,,/Resources/Images/TableSmith.ico"
                      Height="620"
                      Width="900"
                      MinHeight="480"
                      MinWidth="720"
                      WindowStartupLocation="CenterOwner"
                      GlowBrush="DeepSkyBlue"
                      BorderThickness="1"
                      Background="#F7F9FC">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox Text="{Binding SqlText, Mode=OneWay}"
                 FontFamily="Consolas"
                 FontSize="14"
                 IsReadOnly="True"
                 TextWrapping="NoWrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="閉じる" Width="100" Click="CloseButton_Click" />
        </StackPanel>
    </Grid>
</Controls:MetroWindow>
